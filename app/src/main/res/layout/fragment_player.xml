<androidx.swiperefreshlayout.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/swipe_refresh"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/colorBackground"
    tools:context=".ui.fragments.PlayerFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/player_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Liquid Background Container -->
        <FrameLayout
            android:id="@+id/player_background"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Toolbar -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            app:layout_constraintEnd_toStartOf="@+id/btn_favorite"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:navigationIcon="@drawable/ic_feather_arrow_left"
            app:navigationIconTint="@android:color/white"
            app:title="Now Playing"
            app:titleTextColor="@android:color/white" />

        <ImageView
            android:id="@+id/btn_favorite"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="12dp"
            android:src="@drawable/ic_feather_heart"
            app:layout_constraintBottom_toBottomOf="@id/toolbar"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/toolbar"
            app:tint="@android:color/white" />

        <!-- Vinyl Chamber -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/vinyl_chamber"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toTopOf="@+id/layout_info"
            app:layout_constraintTop_toBottomOf="@id/toolbar">

            <!-- Unified Rotating Container -->
            <FrameLayout
                android:id="@+id/layout_vinyl_container"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <!-- Vinyl Disc -->
                <ImageView
                    android:id="@+id/img_vinyl_disc"
                    android:layout_width="320dp"
                    android:layout_height="320dp"
                    android:src="@drawable/vinyl_disc" />

                <!-- Small Album Art in center (Synchronized rotation) -->
                <com.google.android.material.card.MaterialCardView
                    android:id="@+id/card_circle_art"
                    android:layout_width="140dp"
                    android:layout_height="140dp"
                    android:layout_gravity="center"
                    app:cardCornerRadius="70dp"
                    app:cardElevation="0dp"
                    app:strokeWidth="0dp">

                    <ImageView
                        android:id="@+id/img_album_art"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:contentDescription="Album Art"
                        android:scaleType="centerCrop" />
                </com.google.android.material.card.MaterialCardView>
            </FrameLayout>

            <!-- Spindle (Black Dot) - Fixed, does not rotate -->
            <ImageView
                android:id="@+id/img_spindle"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/vinyl_spindle"
                android:elevation="4dp"
                app:layout_constraintBottom_toBottomOf="@+id/layout_vinyl_container"
                app:layout_constraintEnd_toEndOf="@+id/layout_vinyl_container"
                app:layout_constraintStart_toStartOf="@+id/layout_vinyl_container"
                app:layout_constraintTop_toTopOf="@+id/layout_vinyl_container" />

            <!-- Tonearm (Longer Design - Repositioned Higher) -->
            <ImageView
                android:id="@+id/img_tonearm"
                android:layout_width="120dp"
                android:layout_height="240dp"
                android:layout_marginTop="15dp"
                android:layout_marginEnd="-30dp"
                android:rotation="0"
                android:src="@drawable/vinyl_tonearm"
                android:transformPivotX="60dp"
                android:transformPivotY="30dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Track Info -->
        <LinearLayout
            android:id="@+id/layout_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="32dp"
            android:layout_marginBottom="24dp"
            android:gravity="center"
            android:orientation="vertical"
            app:layout_constraintBottom_toTopOf="@+id/card_controls">

            <TextView
                android:id="@+id/text_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="marquee"
                android:gravity="center"
                android:marqueeRepeatLimit="marquee_forever"
                android:singleLine="true"
                android:textAppearance="?attr/textAppearanceHeadlineSmall"
                android:textColor="@android:color/white"
                android:textStyle="bold"
                tools:text="Song Title" />

            <TextView
                android:id="@+id/text_artist"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:gravity="center"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textColor="#88FFFFFF"
                tools:text="Artist Name" />
        </LinearLayout>

        <!-- Controls Panel (Glassmorphism) -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/card_controls"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="24dp"
            android:layout_marginBottom="32dp"
            android:background="@drawable/bg_glass_panel"
            android:paddingHorizontal="24dp"
            android:paddingVertical="24dp"
            android:clipChildren="false"
            android:clipToPadding="false"
            app:layout_constraintBottom_toBottomOf="parent">

            <!-- Shuffle -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/container_shuffle"
                android:layout_width="40dp"
                android:layout_height="40dp"
                app:cardCornerRadius="20dp"
                app:cardElevation="0dp"
                app:cardBackgroundColor="@android:color/transparent"
                app:layout_constraintBottom_toBottomOf="@+id/fab_play_pause"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="@+id/fab_play_pause"
                android:clickable="true"
                android:focusable="true">

                <ImageView
                    android:id="@+id/btn_shuffle"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="center"
                    android:contentDescription="Shuffle"
                    android:src="@drawable/ic_feather_shuffle"
                    app:tint="@android:color/white" />
            </com.google.android.material.card.MaterialCardView>

            <!-- Previous -->
            <ImageView
                android:id="@+id/btn_previous"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:contentDescription="Previous"
                android:src="@drawable/ic_feather_skip_back"
                app:layout_constraintBottom_toBottomOf="@+id/fab_play_pause"
                app:layout_constraintEnd_toStartOf="@+id/fab_play_pause"
                app:layout_constraintStart_toEndOf="@+id/container_shuffle"
                app:layout_constraintTop_toTopOf="@+id/fab_play_pause"
                app:tint="@android:color/white" />

            <!-- Play/Pause (Center) -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/fab_play_pause"
                android:layout_width="64dp"
                android:layout_height="64dp"
                app:cardCornerRadius="32dp"
                app:cardElevation="0dp"
                app:contentPadding="0dp"
                app:cardPreventCornerOverlap="false"
                app:cardUseCompatPadding="false"
                app:cardBackgroundColor="@android:color/transparent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                android:clickable="true"
                android:focusable="true">

                <ImageView
                    android:id="@+id/img_play_pause"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="center"
                    android:scaleType="fitCenter"
                    android:contentDescription="Play"
                    android:src="@drawable/ic_feather_play"
                    app:tint="@android:color/white" />
            </com.google.android.material.card.MaterialCardView>

            <!-- Next -->
            <ImageView
                android:id="@+id/btn_next"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:contentDescription="Next"
                android:src="@drawable/ic_feather_skip_forward"
                app:layout_constraintBottom_toBottomOf="@+id/fab_play_pause"
                app:layout_constraintEnd_toStartOf="@+id/container_repeat"
                app:layout_constraintStart_toEndOf="@+id/fab_play_pause"
                app:layout_constraintTop_toTopOf="@+id/fab_play_pause"
                app:tint="@android:color/white" />

            <!-- Repeat -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/container_repeat"
                android:layout_width="40dp"
                android:layout_height="40dp"
                app:cardCornerRadius="20dp"
                app:cardElevation="0dp"
                app:cardBackgroundColor="@android:color/transparent"
                app:layout_constraintBottom_toBottomOf="@+id/fab_play_pause"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="@+id/fab_play_pause"
                android:clickable="true"
                android:focusable="true">

                <ImageView
                    android:id="@+id/btn_repeat"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_gravity="center"
                    android:contentDescription="Repeat"
                    android:src="@drawable/ic_feather_repeat"
                    app:tint="@android:color/white" />
            </com.google.android.material.card.MaterialCardView>

            <!-- Bottom Row: Time and Progress -->
            <TextView
                android:id="@+id/text_current_time"
                android:layout_width="45dp"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="00:00"
                android:textColor="#99FFFFFF"
                android:textSize="12sp"
                app:layout_constraintBottom_toBottomOf="@+id/seekbar_progress"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="@+id/seekbar_progress" />

            <androidx.appcompat.widget.AppCompatSeekBar
                android:id="@+id/seekbar_progress"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="12dp"
                android:layout_marginTop="24dp"
                android:maxHeight="4dp"
                android:minHeight="4dp"
                android:paddingStart="7dp"
                android:paddingEnd="7dp"
                android:progressDrawable="@drawable/glass_seekbar_track"
                android:splitTrack="false"
                android:thumb="@drawable/glass_seekbar_thumb"
                android:thumbOffset="0dp"
                app:layout_constraintEnd_toStartOf="@+id/text_total_time"
                app:layout_constraintStart_toEndOf="@+id/text_current_time"
                app:layout_constraintTop_toBottomOf="@+id/fab_play_pause" />

            <TextView
                android:id="@+id/text_total_time"
                android:layout_width="45dp"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="00:00"
                android:textColor="#99FFFFFF"
                android:textSize="12sp"
                app:layout_constraintBottom_toBottomOf="@+id/seekbar_progress"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="@+id/seekbar_progress" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
